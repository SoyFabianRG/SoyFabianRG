name: Generate Snake

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch: # Permite la ejecución manual
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#cba6f7&color_dots=#313244,#45475a,#585b70,#6c7086,#94e2d5&color_background=#1e1e2e
            dist/github-contribution-grid-snake-dark.svg?color_snake=#cba6f7&color_dots=#1e1e2e,#313244,#45475a,#6c7086,#94e2d5&color_background=#11111b

      - uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
